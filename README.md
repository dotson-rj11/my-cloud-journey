## Networking Skills Verified
* Successfully mapped local hostnames to public IPs via /etc/hosts.
* Verified Layer 4 connectivity using Netcat (nc).
* Analyzed global routing paths using traceroute.
## Security Skills Verified
* Implemented User Access Control and verified permissions for restricted users. 
* Verified that the 'visitor' user is restricted from private directories. 
